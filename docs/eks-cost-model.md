# EKS 비용 모델 — 실측 확정 🔴 재조사 불필요

> **출처**: `.claude/CONTEXT.md` 에서 이관 (2026-09-18). CONTEXT 가 1017줄로 80줄 규칙을 12.7배
> 위반해 주제별로 재배치했다(원장 L-10). 이 문서는 **참조 자료**다 — 세션 상태가 아니라 상수를 담는다.
>
> ⚠️ **결정 블록(📌 D-001·D-002·D-004)은 여기 없다.** `infra/aws-eks/README.md` 에 있다 —
> `check-design-integrity.sh` 의 `DOCS` 배열이 **고정 목록**이라 새 문서는 무결성 검사 밖이고,
> *"결정이 사는 곳이 감시 밖이면 무결성 검사의 의미가 없다"*(그 스크립트의 자기 주석).

## 단가 (ap-northeast-2, AWS Pricing API·Spot 이력 직접 조회 — 추정 아님)

| 항목 | 실측 단가 | 비고 |
|------|----------|------|
| EKS 컨트롤플레인 | **$0.10/hr** = $73/월 | 워크로드 무관 **고정비** |
| ALB | $0.0225/hr · LCU $0.008/LCU-hr | + 퍼블릭 IP ×2 AZ |
| 퍼블릭 IPv4 | **$0.005/hr** = $3.65/월/개 | 노드마다 1개 — **NAT 회피 설계의 대가** |
| EBS gp3 | **$0.0912/GB-Mo** | 기본 3000 IOPS·125MB/s 무료 |
| EBS 스냅샷 | $0.05/GB-Mo | 볼륨 유지보다 45% 싸나 복원 마찰 → 비추 |
| ECR 스토리지 | **$0.10/GB-Mo** | |
| t4g.small 스팟 | 평균 $0.00902 / 최고 $0.01090 | 7일 **130샘플** |
| t4g.medium 스팟 | 평균 $0.01923 / 최고 **$0.03210** | 7일 **127샘플** |
| t4g.medium 온디맨드 | **$0.0416/hr** | ⚠️ *"스팟의 2.2배"* 라고 적었었는데 **부분 무효** — `plans/2026-08-03-…phase02.md:176` 이 *"small 의 정확히 2배(= 2×small 과 동일 비용)"* 로 실측 정정했고, 그게 D-009 의 노드 기각을 무효화했다 |

> 🔴 **스팟 실측(130·127샘플)은 이 문서에만 있다.** 이 계정은 Spot 쿼터가 0 이라 당장 못 쓰지만,
> 쿼터 증액 시 **유일한 기준선**이고 재조사하려면 Pricing/Spot 이력 조회를 다시 해야 한다.

## 🔴 고정비가 지배한다

```
노드 0대여도  컨트롤플레인 $73 + ALB 세트 $29.6 = 월 $102.57
```

***"인스턴스를 줄여서 버티기" 가 통하지 않는 구조다.*** 이 산술이 없으면 *"노드만 줄이자"* 가
되살아난다(D-002 재채택 경로).

## 확정 전략 — destroy-after-use

버퍼(스팟 최고가 + 잡비 10%) 적용 시간당 단가:

| 모드 | 시간당 | 용도 |
|------|-------|------|
| t4g.small ×1, ALB 없음 | **$0.13** | 인프라 학습(NetworkPolicy·RBAC·Helm·ArgoCD) — nginx 파드로 충분 |
| t4g.medium ×1, ALB 없음 | $0.16 | 실앱 배포 |
| **t4g.medium ×2 + ALB (풀 3서비스)** | **$0.25** | Phase 3 토폴로지 |

**주 25시간 × 26주(650h)**: 인프라 학습 위주 **$110** · 항상 풀 토폴로지 **$163**.
→ 6개월 내내 헤비하게 써도 크레딧이 남는다. 다 태우려면 **주 38시간**이 필요하다(비현실적).

- **삽질 비용**: 클러스터 완전 재생성 ≈ **$0.07** / 4시간 세션 $0.64 / 8시간 삽질 $1.28.
  ***실패는 사실상 공짜다 — 아낄 것은 크레딧이 아니라 "켜놓고 딴짓하는 시간".***
- **절감 레버**: ①ALB 는 필요할 때만 ②인프라 학습은 t4g.small ③세션을 **길게 가끔**
  (생성 10~15분 + 삭제 10분 = 회당 25분 오버헤드 상각) ④kind 부활 불필요(돈이 제약이 아니다)
- **잔액 활용**: 남는 크레딧으로 **막판 기간 한정 상시 데모**(3~6주)를 사서 구직·면접 시즌에 맞춘다.
  끝나면 destroy → 영구 비용 0, prod 는 Fly 복귀.

> 🔴 **2026-09-16 반례**: 이 모델은 **세션이 정상 종료한다**고 가정한다. #426 에서 `destroy` 를
> 배경에 두고 완료를 확인하지 않아 **15.8시간**이 샜고, 예상 $0.13 → 실제 **$2.77~3.14**(21~24배)였다.
> ***비용은 무엇을 켜느냐가 아니라 어떻게 끄느냐가 정한다.*** → `docs/eks-session-sop.md` §5·§8b.

## 가드레일 (현재 구성)

| 장치 | 설정 |
|---|---|
| **Budgets ×2** (`credit-010-100`, `credit-110-200`) | 누적 크레딧 **$10 단위 20단계**. AWS 하드 리밋이 *예산 1개당 알림 10개*라 둘로 쪼갰다 |
| **Cost Anomaly** (`devquest-eks-service-monitor`) | **DAILY · $5** |

> 🔴 **종전 서술 `$10/$50/$150 ABSOLUTE_VALUE` 는 무효다** — 2026-07-31 에 위 구성으로 교체됐는데
> CONTEXT 가 갱신되지 않아 **1년 가까이 틀린 채로 남아 있었다**(2026-09-18 발견·정정).
> 실물은 `infra/aws-eks/0-bootstrap/budget.tf` · 인벤토리는 `infra/aws-eks/PERSISTENT-RESOURCES.md`.
>
> 🔑 **두 갈래인 이유**: 예산은 **누적** $10 단위라 주말 방치 같은 하루 급증($6)은 다음 계단을
> 안 넘으면 안 울린다. 하루 단위 급증을 잡는 것은 **이상탐지(DAILY $5)** 다.
> ~~"$35 예산 알람"~~ 이라는 서술이 SOP 에도 있었는데 **그런 알람은 존재한 적이 없다**(07-31 정정).

## destroy 시 데이터 생존 (코드 전수 확인)

| 데이터 | 소속 | destroy 시 |
|--------|------|-----------|
| 앱 DB (Neon Postgres) | AWS 밖 외부 SaaS | ✅ 완전 유지 |
| tfstate / VPC·서브넷 | 0-bootstrap / 1-network | ✅ 유지 (destroy 대상은 `2-cluster` 뿐) |
| K8s 매니페스트·ArgoCD | git | ✅ 유지 |
| Grafana 대시보드·메트릭 | Grafana Cloud | ✅ 유지 |
| 영속 EBS | 0-bootstrap | ✅ 유지 (D-004) |
| ECR 이미지 | 0-bootstrap | ✅ 유지 (lifecycle 10개) |

- **NAT·EIP 가 코드 어디에도 없다**(전수 확인) + 컨트롤플레인 로깅 OFF → **destroy 후 잔존 비용 ≈ $0.**
  NAT 회피 설계가 여기서 값을 한다(있었으면 월 $32 계속).
- 🔴 **destroy-after-use 가 실패하는 진짜 경로**: `tofu destroy` 는 **state 에 있는 것만** 지운다.
  K8s 가 만든 **ALB(Ingress)·EBS(PVC)·NLB 는 state 에 없어 고아로 남아 계속 과금**된다.
  **비용 알람이 울리면 1순위 용의자가 고아 ALB**(월 $16.43 + IP $7.30). 절차는 SOP §8.

## 앱은 영속 볼륨이 필요 없다 (전수 확인, 2026-07-22)

EBS 는 **순수 학습 목적**이다. BE 코드 grep: `MultipartFile`·`Files.write`/`FileOutputStream`·
S3 클라이언트·로컬 디스크 경로 설정 **전부 0건** — 파일을 디스크에 쓰는 코드가 아예 없다.
(2026-09-18 재확인: `MultipartFile` grep 여전히 **0건**)

| 데이터 | 실제 저장소 | EBS 필요? |
|--------|------------|:---:|
| 앱 DB (사용자·퀘스트·진행도·AI 평가·`ai_call_log`) | Neon (AWS 밖) | ❌ |
| 컨테이너 이미지 | ECR | ❌ |
| K8s 매니페스트·ArgoCD 설정 | git (GitOps) | ❌ |
| 메트릭·로그 | Grafana Cloud (OTLP push) | ❌ |
| 프롬프트 `.st`·`conference-references.json` | jar 내부 리소스(읽기 전용) | ❌ |
| 이력서 PDF | **FE 에서 pdfjs 파싱** → 텍스트만 전송, 파일 저장 없음 | ❌ |
| Terraform state | S3 | ❌ |

ArgoCD 기본 설치도 PVC 미사용(상태는 CRD, repo-server 는 emptyDir), Prometheus 는 in-cluster 가
아니라 Grafana Cloud push → **인프라 컴포넌트도 영속 볼륨을 요구하지 않는다.**

> 🔑 이 표가 없으면 *"EBS 가 앱에 필요하다"* 는 오해로 **영속 볼륨 확대가 정당화된다.**

## 학습 IaC → 실서비스 전환

**골격 100% 재사용.** 학습용으로 낮춘 것이 `cluster.tf` 주석에 근거와 함께 명시돼 있어
**prod 전환 체크리스트가 이미 코드에 있다.** 바꿀 곳 7개:

| # | 현재(학습) | prod 전환 | 비용 영향 |
|---|-----------|----------|----------|
| 1 | `endpoint_public_access=true`, `0.0.0.0/0` | CIDR 제한 / private | $0 |
| 2 | 컨트롤플레인 로깅 생략 | `enabled_cluster_log_types` | CloudWatch 요금 |
| 3 | secret KMS 암호화 생략 | KMS 암호화 | KMS $1/월+ |
| 4 | `capacity_type` **변수화 완료**(기본 `ON_DEMAND`) | tfvars 로 SPOT 주입(쿼터 증액 후) | 노드비 2.2배 |
| 5 | 노드가 **퍼블릭 서브넷**+공인IP | 프라이빗 서브넷+NAT | **+$32/월** |
| 6 | `desired_size=1` | 2+ (다중 AZ HA) | 노드비 ×N |
| 7 | CI IAM 이 admin 정책 | 최소권한 | $0 |

- **이미 변수화**(tfvars 만 교체): 인스턴스 타입·노드 수·K8s 버전·클러스터명·`capacity_type`
- **하드코딩이라 코드 수정 필요**: `ami_type`·서브넷 선택·엔드포인트 설정
- 즉 *"학습 IaC 를 그대로 prod 에"* 가 아니라 **"같은 골격에 prod 파라미터를 끼운다"** —
  레이어 분리·remote backend·OIDC·IRSA·애드온 배선은 **전부 그대로 간다.**

> 📌 선행 조건 상세는 `docs/superpowers/plans/2026-09-11-prod-eks-migration-prereqs.md` (B-17).

## 🔴 Free Plan — 계정 폐쇄 트리거 (2026-07-23, API·공식문서 3자 대조)

| 확인 항목 | 결과 |
|-----------|------|
| 계정 플랜 | `FREE` · 크레딧 **$199.81** 로 시작 |
| 만료일 | **2027-01-15** |
| EKS 가 Free Plan 제한 대상? | **아니다** (제한 예시는 Savings Plans·RI·일부 Marketplace) |
| 초과 과금? | **없다** — 대신 아래 폐쇄 |

- 🔴 **폐쇄 트리거 2개**: *"Account closes when credits are depleted **OR** when the plan duration ends."*
  **만료일만이 아니라 크레딧 소진도 즉시 계정 폐쇄**다. 한국어 랜딩은 이 문장을 통째로 누락해
  *"요금 안 나감"* 만 강조한다. ***돈이 아니라 계정이 대가.*** (폐쇄 후 90일 content 보관)
  → **안전 예비 $30 규칙** — 사용 가능액 $170. 단 **"다 태우기"를 목표로 삼지 말 것.**
  🔑 **prod 는 Fly+Neon 이라 계정 폐쇄돼도 무영향이다** — 이 문장이 $30 규칙을 *"파산 방지"* 가
  아니라 *"학습 자원 관리"* 로 만든다. (2026-09-18 복원: #427 이관 중 이 한 줄이 탈락했고
  QA F-7 이 잡았다 — 계획서가 이 문장을 인용하는데 인용 대상에 없었다.)
  > 🔴 **이 문장은 prod 가 Fly 에 있는 동안만 참이다 (2026-09-22 추가).**
  > `plans/2026-09-11-prod-eks-migration-prereqs.md` 의 **0번 항목**(이관 계획서)이 정확히
  > 이것을 뒤집는다 — prod 가 이 계정으로 들어오면 **계정 폐쇄 = prod 소멸**이 되고,
  > $30 규칙은 *"학습 자원 관리"* 에서 *"파산 방지"* 로 성질이 바뀐다.
  > 실측: 잔여 ≈$194 ÷ 상시 $140/월 ≈ **1.4개월**. 📌 **D-002** 가 같은 값을
  > *"크레딧 수명 5~7주"* 로 이미 적어뒀고, 상태는 아직 `✅유효`(= 상시 운영 **기각**)다.
- 🟡 **Spot vCPU 쿼터 0 은 Free Plan 제한이 아니라 신규 계정 기본값.** 단 스팟↔온디맨드 650h 차이가
  **$13뿐**이고 회수 중단이 사라지니 **온디맨드가 낫다**(`nodes.tf` 기본값 `ON_DEMAND`).
- 🟡 **자동 Paid 전환 트리거**: AWS Organizations 가입·Control Tower·Partner Network·Enterprise
  Agreement. **멀티계정 실습으로 Organizations 를 만지면 그 순간 Paid** 가 된다.
- 🟢 **잔여 크레딧 이월**: Paid 업그레이드해도 남은 크레딧이 future bill 에 적용된다.

> 📅 만료일 캘린더 등록은 `.claude/TASKS.md` TASK-6 소관.
> 💰 **현재 소진액은 여기 적지 않는다** — `aws budgets describe-budgets`(무료)로 파생된다.
> CONTEXT 에 적어뒀던 `$2.936` 은 #426 미반영으로 이미 썩어 있었다(실제 ≈$5.7~6.1).
