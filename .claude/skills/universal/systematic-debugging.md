# SKILL: Systematic Debugging (체계적 디버깅)

**트리거:** 버그, 테스트 실패, 예상치 못한 동작 — 모두

## 철의 법칙

```
근본 원인 조사 없이는 수정 없다
```

"빠른 수정" 생각이 드는 순간이 가장 위험하다. 특히 긴급 상황일수록 체계적 접근이 더 빠르다.

## 4단계 프로세스

**Phase 1: 근본 원인 조사 (수정 전 필수)**
1. 에러 메시지 완전히 읽기 (스택 트레이스 포함)
2. 재현 가능하게 만들기
3. 최근 변경사항 확인 (`git diff`, 최근 커밋)
4. 여러 컴포넌트가 있다면 → 각 경계에 진단 로그 추가 후 실행하여 어디서 깨지는지 확인

**Phase 2: 패턴 분석**
- 작동하는 유사한 코드 찾기
- 레퍼런스 구현과 비교 (완전히 읽기, 스킵 금지)
- 차이점 목록화

**Phase 3: 가설 및 테스트**
- 단일 가설 수립: "X가 근본 원인이다. 왜냐하면 Y"
- 가장 작은 변경으로 테스트
- 한 번에 하나씩만

**Phase 4: 구현**
- 실패하는 테스트 케이스 먼저 작성 (TDD 적용)
- 단일 수정
- 검증

## 3번 이상 수정 실패 시

수정을 계속 시도하지 말고 **아키텍처 자체를 재검토**하라.
각 수정이 다른 곳의 새 문제를 드러내고 있다면 → 근본적으로 잘못된 구조다.
막히면 즉시 사용자에게 보고하고 방향을 묻는다.

## 빨간 깃발

- "일단 이거 바꿔보고..." → Phase 1로 돌아가라
- 원인 이해 없이 여러 수정을 동시에 적용 → 금지
- "아마 X 때문인 것 같다" → 가설을 증거로 검증하라

## 프로젝트 특유 패턴

- **"설정이 없다"고 결론내기 전에 파일 경로부터 확인** — `grep ... foo.toml || echo "없음"`은 **파일이
  없어도 "없음"을 출력**한다. 2026-07-28 사고: 루트 `fly.toml`(존재하지 않음)을 grep하고 "TZ 미설정"으로
  단정 → 실제로는 `be/fly.toml`에 설정돼 있었고, 없는 버그를 진단해 PR까지 머지했다.
  → `ls <경로>`로 존재를 먼저 확인하거나 `grep -r`로 훑을 것.
- 🔴 **일반론(웹 검색·통념)으로 특정 환경의 사실을 대체하지 말 것.** 같은 사고에서 "Alpine엔 tzdata가
  없어 TZ가 무시된다"는 널리 퍼진 서술을 채택했으나, **우리 베이스(`eclipse-temurin:*-alpine`)엔 tzdata가
  있어서** 정반대였다. **기존 결론을 살려주는 근거일수록 더 의심할 것**(동기화된 추론).
  → 컨테이너 사실 확인은 `docker run --rm <image> <명령>` 한 줄, 30초다. 로컬에 colima가 설치돼 있다
  (`colima start` 후 사용).
- 생성자 변경 시 → `@Mock` 누락 여부 즉시 확인
- `verify()` 실패 시 → 파라미터 타입 불일치 확인 (Kotlin null 포함)
- Flyway 오류 시 → V* 버전 번호 중복, `repair()` 선행 여부 확인
- **Flyway 마이그레이션 신규 작성 전 필수**: 이 프로젝트는 마이그레이션이 `be/core/core-api/.../db/migration/`
  와 `be/storage/db-core/.../db/migration/` 두 디렉토리에 분산되어 있고 런타임 클래스패스에서 합쳐진다
  (2026-07-01 PR #231에서 한쪽만 보고 V8을 중복 생성 → prod 부팅 실패로 서버 전체 다운 사고 발생).
  새 버전 번호를 정하기 전 반드시 아래 명령으로 두 디렉토리 전체를 확인할 것:
  ```bash
  find be -path '*/db/migration/V*.sql' | grep -v '/build/' | sort -V
  ```
  BE CI에 이 중복을 자동 차단하는 린트가 추가되어 있으나(`be-ci.yml`), PR 병합 후 배포 실패로
  이어지는 것보다 작성 시점에 직접 확인하는 편이 훨씬 저렴하다.
