# 리뷰 지적 원장 (Review Ledger)

> **이 파일은 커밋된다.** 브랜치가 삭제돼도, 세션이 끊겨도, 머신이 바뀌어도 살아남는 유일한 지적 저장소.
> 브랜치별 작업 파일 `.claude/qa-cache/<브랜치>.findings.md`는 **gitignore = 휘발성**이다.
> 그러니 **PR에서 안 고치기로 한 지적은 반드시 여기로 이관**해야 한다. 안 하면 사라진다.

## 규칙

| | |
|--|--|
| 언제 적나 | 브랜치 findings의 지적이 `deferred`로 판정될 때 — **PR 생성 전에** |
| 누가 적나 | orchestrator (qa-reviewer는 브랜치 findings까지만 담당) |
| 언제 지우나 | 지우지 않는다. 상태를 `closed`/`wontfix`로 바꾸고 근거를 남긴다 |
| 강제 장치 | `assert-qa-run.sh`가 `deferred` 지적의 원장 등재 여부를 검사 → 누락 시 PR 차단 |

**상태**: `open`(미처리) · `closed`(해소, 근거 필수) · `wontfix`(안 고치기로 확정, 근거 필수) · `obsolete`(코드가 바뀌어 무의미)

**출처 표기**: `<브랜치>/<F-ID>` — 훅이 이 문자열로 등재 여부를 찾는다. 형식을 바꾸지 말 것.

---

## 미해결 (open)

| ID | 출처 | 등급 | 내용 | 이관 사유 / 처리 방향 |
|----|------|:----:|------|---------------------|
| L-23 | `fix/harness-holes/F-7` (08-15 QA) | LOW | **`test-guards.sh`의 `MIN_CASES` 하한은 얕은 방어다.** 비의도적 배선 누락(파일명 오타·`run_*` 호출 빠뜨림)은 실제로 잡지만(실측 2회 — 배선 안 된 케이스 파일 발견, awk 필드 오류로 러너가 깨진 것 검출), **케이스 파일과 `MIN_CASES`를 같은 커밋에서 함께 줄이는** 축소는 막지 못한다. 주석 자체가 그 경로를 열어둔다 | **의도적으로 얕게 뒀다.** 이걸 기계로 막으려면 "케이스가 왜 줄었는가"를 판정해야 하는데 그건 코드 리뷰의 일이다. 🔑 실질 방어는 두 겹이다: ①하한이 **비의도적** 누락을 잡는다(실측으로 두 번 작동) ②의도적 축소는 diff에 `MIN_CASES` 변경이 남아 **리뷰에서 보인다**. ⚠️ 트리거: `MIN_CASES`를 낮추는 커밋이 근거 없이 올라오면 그때 강화를 재검토 |
| L-22 | `fix/harness-holes/F-5` · `fix/harness-holes/F-6` · `fix/harness-holes/F-10` (08-15 QA) | LOW | **`assert-qa-readonly.sh`는 허용목록 명령의 *인자* 속 실행 지시를 못 본다.** 세그먼트당 맨 앞 토큰만 검사하므로 `find . -exec rm -rf {} \;` · `echo x &#124; xargs rm -rf` · `awk 'BEGIN{system("rm -rf be/")}'` · 프로세스 치환 `diff <(rm -rf be/core)` 가 전부 통과한다(QA 코드 추적으로 확인) | 🔴 **wontfix — L-12와 같은 판정.** 정적 텍스트 검사의 구조적 한계이지 구현 결함이 아니다. 3라운드에 걸쳐 조였고 매번 새 우회가 열렸다(따옴표 → 겹따옴표 안 `$(...)` → ANSI-C 인용 → 인자 속 실행). 파서를 더 정교하게 만드는 것은 *"3번 시도했는데 4번 더"* 다. 🔑 **위협 모델**: 이 가드가 막는 것은 **LLM 리뷰어가 무심코 고치는 것**이고 그 형태(`echo > file`·`sed -i`·`git checkout`)는 전부 잡힌다. `find -exec`·프로세스 치환은 **작정해야 나오는 형태**이고, 작정한 리뷰어는 어차피 보고서에 거짓을 쓸 수 있다 — 그건 다른 층위의 문제다. ✅ **대신 우회 불가능한 검사를 추가했다** — `qa-effect-guard.sh`(SubagentStart/Stop)가 **결과**를 본다. 명령을 파싱하지 않으므로 어떤 셸 트릭을 쓰든 파일이 바뀌면 잡힌다. 반증: F-6 형태로 실제 수정 → 검출 확인. ⚠️ 자동 되돌리기는 하지 않는다(같은 워킹트리의 orchestrator 작업 파괴 위험) <br><br>📌 **2026-08-16 위협 모델 확정(사용자 결정).** 이 가드는 **실수 탐지기**이지 적대자 통제가 아니다 — 근거: **적대적 리뷰어는 파일을 고칠 필요가 없다, 보고서에 거짓을 쓰면 된다.** QA 전체가 리뷰어의 정직성에 의존하므로 파일시스템만 잠그는 것은 범주 오류다. 따라서 F-5·F-6·F-10(인자 속 실행·프로세스 치환·명령 내장 파일쓰기)은 **범위 밖**이다. ⚠️ 이 모델을 근거 없이 넓히지 말 것 — 넓히면 파서를 계속 조이게 되고 조일 때마다 새 우회가 열린다(4라운드 실측). 적대자를 정말 막아야 한다면 답은 파서가 아니라 **격리**(읽기 전용 마운트·별도 사용자)다 <br><br>✅ **wontfix — 2026-08-16 사용자 결정.** 위협 모델을 **실수 탐지기**로 확정하고 (근거: *적대적 리뷰어는 파일을 고칠 필요가 없다 — 보고서에 거짓을 쓰면 된다*), 이 세 건이 그 범위 밖임을 사용자가 판단했다. F-10의 최고 결과 지점(gitignore된 `settings.local.json` = 권한 343건)만 **파서를 넓히지 않고 고정 감시 목록**으로 완화했다(`qa-effect-guard.sh`의 `WATCH`). 🔑 재검토 트리거: **위협 모델이 바뀌면**(예: 리뷰어를 신뢰 경계 밖으로 옮기면) 즉시 재판정. <!-- USER-DECIDED --> |
| L-19 | `stage/eks-l14-l15-verify` (08-12 유료 세션 실측) | LOW | **readiness는 503이 아니라 *타임아웃*으로 실패한다.** DB 정지 시 kubelet 이벤트가 `context deadline exceeded (Client.Timeout exceeded while awaiting headers)` — Spring `db` 헬스 인디케이터가 커넥션 획득에서 **블로킹**해 응답 자체가 없다. 즉 코드·문서가 함의하는 *"DB를 보고 503을 준다"* 가 아니라 *"매달려서 죽는다"* 다 | **결함이 아니다 — 실제로 동작한다**(kubelet은 타임아웃도 실패로 계산, T+30에 NotReady 실측). 다만 **문서화되지 않은 의존**이 3개 있다: ①`timeoutSeconds: 1`이라 빨리 잡히는 것이 **우연에 가깝다**(5s로 늘리면 감지가 그만큼 느려짐) ②503 본문을 파싱하는 외부 모니터링은 아무것도 못 받는다 ③Hikari `connection-timeout`(기본 30s)에 묶여 있어 그 값을 늘리면 헬스 응답도 같이 느려진다. 🔑 **트리거: `timeoutSeconds`·Hikari `connection-timeout`을 바꾸거나 헬스 기반 알림을 붙일 때 재검토.** 고친다면 헬스체크 전용 짧은 타임아웃을 주는 방향 |
| L-16 | `fix/readiness-probe-actually-checks/F-2` | LOW | **readiness 그룹 커스텀 `include: db,ping`가 Boot 기본 `readinessState`를 배제한다.** `readinessState`는 graceful shutdown 시 kubelet에 *"이제 트래픽 주지 마라"* 를 알리는 신호다 | **현재는 무해**하다 — `server.shutdown: graceful` 미설정 + 배포 전략이 `Recreate`(구 파드를 먼저 종료하므로 드레인 조율이 필요 없다). 다만 `k8s/base/core-api.yaml` 주석이 *"실무 prod는 최소 2 + PodDisruptionBudget"* 을 언급하고 있어, **`RollingUpdate` + graceful shutdown을 도입하는 순간** 이 배제가 조용히 *"probe가 진짜 상태를 못 본다"* 를 재현한다. 🔑 **트리거: `server.shutdown=graceful` 또는 `strategy: RollingUpdate` 도입 시 재검토** |
| L-17 | `fix/readiness-probe-actually-checks/F-3` | LOW | **로컬 `./gradlew`가 `JAVA_HOME` 미설정이면 실패한다.** Homebrew `openjdk@21`이 설치돼 있으나 시스템에 링크되지 않아 `Unable to locate a Java Runtime`. CI는 정상 | **이 PR이 만든 문제가 아니다**(diff에 toolchain 변경 없음). 다만 **서브에이전트·새 사람이 표준 명령을 그대로 못 쓴다** — `be/build.gradle.kts`가 colima `DOCKER_HOST`를 조건부 주입해 같은 종류의 문제를 해결한 **선례가 있다**(#364). 같은 패턴으로 toolchain auto-detection을 넣는 별도 chore. ⚠️ 전역 빌드 파일 변경이라 blast radius가 있어 이 PR에 섞지 않는다<br><br>📊 **08-14 재발 — 서브에이전트가 실제로 밟았다.** be-feature-builder가 L-3·L-4 작업 중 `./gradlew`가 죽어 스스로 `export JAVA_HOME=/opt/homebrew/opt/openjdk@21`을 해야 했다. **매 위임마다 이 우회를 반복하게 된다** — 프롬프트에 적어두지 않으면 에이전트가 무출력으로 실패한다(실제로 이번엔 프롬프트에 경고를 넣어서 넘겼다). 🔑 트리거 상향: *"스크립트가 하나 더 늘 때"* 가 아니라 **다음 BE 위임 때 바로** 처리 |
| L-18 | `fix/readiness-probe-actually-checks/S-3` | LOW | **`assert-qa-run.sh`의 SHA 일치 검사에 순환이 있다.** 원장의 `<ID> → closed` 표시는 **QA 통과의 결과**라 논리적으로 최종 QA보다 앞설 수 없는데, 훅은 예외 없는 HEAD==마커 일치를 요구한다. 결과적으로 ①문서 한 줄 때문에 QA를 한 번 더 돌리거나 ②PR 생성 후 push하거나 둘 중 하나가 된다(#374는 ②를 택함) | **의도적으로 이번에 안 고쳤다** — 훅을 느슨하게 만드는 변경(예: 문서 전용 diff 면제)은 **게이트를 우회하는 구멍**이 될 수 있고, 이 레포는 그런 완화를 3번 시도했다 실패한 선례가 있다(L-12). 실제 피해는 *"PR 생성 후 doc 커밋 1개"* 로 작고, **그 커밋이 리뷰를 건너뛴 코드를 담지 않는다는 보장**을 어떻게 기계로 만들지가 진짜 문제다. 🔑 **트리거: 이 패턴이 3회 이상 반복되면** 설계를 다시 본다(예: 원장 종료를 별도 파일로 빼 SHA 검사 대상에서 제외)<br><br>📊 **실측 2/3 (2026-08-14).** 최근 머지 14건 중 *PR 생성 시각 < 마지막 커밋 시각*인 것: **#374, #382** 2건. 재는 법(다음에 재도출하지 말 것): `gh pr list --state merged --limit N --json number,createdAt,commits` 로 `createdAt` vs `commits[-1].committedDate` 비교. **한 번 더 발생하면 트리거** <br><br>🔴 **2026-08-16 트리거 도달 — 4/20.** 최근 머지 20건 중 PR 생성 후 커밋이 추가된 것: #364·#367·#374·#382. 기록해둔 트리거가 *"3회 이상"* 이었다. 🔑 **다만 원인 분석을 정정한다.** 오늘 `fix/harness-holes`에서 QA를 5라운드 돌았는데 그건 마찰이 아니었다 — 매 라운드 **실제 코드를 고쳐 커밋**했으므로 SHA가 움직인 것도 재실행도 옳았다. 진짜 마찰은 좁다: **QA가 요구한 뒷정리(원장 등재·findings 상태 갱신)만 담은 커밋**이 SHA를 어긋나게 하는 경우. 정의상 코드가 아니다. ➡️ **처리 방향(다음 세션 전용 PR)**: 마커 제거가 아니라 **좁은 예외**. `marker..HEAD` diff가 `.claude/review-ledger.md` · `.claude/CONTEXT.md` · `.claude/qa-cache/**` 만 건드리면 통과. `git diff --name-only` 로 기계 검증 가능하고, 코드가 한 줄이라도 섞이면 그대로 차단된다. ⚠️ **존재 검사(마커 유무)는 남긴다** — 그게 QA를 반드시 돌게 만드는 유일한 기계 장치다. 없애면 *"orchestrator.md 7단계에 적혀 있다"* 로 돌아가고, 이 레포에서 산문 규칙은 반복해서 죽었다(mneme 41h 0건, 학습 설명 의무 → 퀴즈 훅 대체). ⚠️ 완화는 **반드시 별도 PR** — 이 레포는 게이트 완화를 3번 시도했다 실패한 전례가 있다(L-12) |
| L-1 | `feat/phase1-ai-call-log-observability/F-2` | MEDIUM | **`ai.call.log.recorded{success=false}` 태그가 도달 불가.** `CacheMetricsAdvisor`가 정상 응답일 때만 `record()`를 부르고 `success = true`로 하드코딩한다(실패 경로는 조기 return). 어댑터 로직은 옳지만 운영에서 이 태그는 영원히 0이다 | client-ai 수정 = inprocess 경로 동작 변경이라 Phase 1의 "동작 불변" 원칙에 반함. **AI 실패율 관측은 Task 1.4의 HTTP 어댑터 에러 매핑으로 확보**하기로 했고 부분 확보됨. advisor 자체 개선은 **Phase 3 정리 대상** |
| L-2 | `feat/phase1-ai-api-rest-controllers/F-4` | LOW | `TechInterviewWireFormatContractTest`에 **wire format 계약과 에러 경로 계약이 혼재**. 파일명이 내용을 정확히 표현하지 못함 | 동일 `@SpringBootTest` 컨텍스트 재사용이 목적이라 기능 문제 없음. 리네임(`TechInterviewHttpContractTest` 등) 권고 수준 |
| L-5 | `feat/phase1-ai-api-config-migration/F-2` | LOW | `AiConfigBindingTest`가 `Environment.getProperty` 기반이라, 실제 빈(`AiCallExecutor.maxRetry`)의 주입값 확인보다 **한 단계 간접적** | 동일 병합 property source를 조회하므로 실질 위험 없음. 더 강한 검증으로 올릴 여지만 있음 |
| L-6 | `feat/phase1-core-http-adapter/F-3` | LOW | `BaseAiHttpAdapter`가 응답을 항상 `String`으로 받은 뒤 파싱 → **메모리 이중화**(String + 파싱된 객체) | 406 회피를 위한 의도적 설계. AI 평가 결과 JSON 크기에선 실용상 무해. 응답이 커지면 재검토 |
| L-8 | `refactor/jackson3-db-core/F-2` | LOW | **root `build.gradle.kts`의 subprojects 블록이 전역 J2 `jackson-module-kotlin`을 모든 모듈에 적용** + db-core는 `spring-boot-starter-json`(J2 계열)까지 있어 J2/J3 kotlin 모듈이 클래스패스에 공존(dead weight). 소스 참조는 이미 J3 0건 확인됨 | **전역 빌드 변경이라 blast radius가 커서 이번 PR 범위에서 의도적으로 제외.** 후속으로 root subprojects J2 모듈 제거 + db-core를 `spring-boot-starter-jackson`으로 전환. → CONTEXT 백로그가 실행 항목 소유 |
| L-12 | `stage/eks-3b-postgres-static-pv/F-7` | LOW | **`assert-no-csi-delete-tags.sh`는 런타임 조립 문자열을 탐지할 수 없다.** QA가 PoC로 실증: `format("%s.csi.%s/cluster","ebs","aws.com")` · `join(".", ["ebs.csi","aws.com/cluster"])` · `base64decode(...)` 셋 다 exit=0(통과)으로 빠져나간다. 문자열이 파일에 물리적으로 존재하지 않으면 grep은 진다 | 🔴 **고치지 않기로 확정 — 정적 텍스트 검사의 구조적 한계이지 구현 결함이 아니다.** 3라운드에 걸쳐 조였고 매번 새 우회가 열렸다(F-3 중괄호 → F-4 computed key → F-5 줄단위 제외 → F-6 .tfvars). 정규식을 더 정교하게 만드는 것은 *"3번 시도했는데 안 되네, 4번 더"* 에 해당한다. **막아야 할 현실적 실패는 선의의 한 줄**(*"CSI가 관리하게 하려면 태그를 붙여야지"*)이고 그건 이미 잡힌다. 의도적 우회자는 어차피 IRSA 정책을 직접 건드린다. **대신 우회 불가능한 검사를 추가했다** — `PERSISTENT-RESOURCES.md` §확인 명령 ③이 배포된 볼륨의 태그를 AWS에 직접 조회한다(최종 상태를 보므로 소스 문법과 무관). 스크립트 상단에도 역할·한계를 명시해 다음 사람이 또 조이지 않게 했다 | 
| L-10 | `stage/eks-3a-postgres-dynamic-pvc/F-3` | LOW | **`.claude/CONTEXT.md`가 765줄로 "80줄 초과 시 archive 이동" 규칙을 크게 위반.** 이 PR이 54줄을 더해 악화시켰다 | 이 PR이 만든 문제가 아니라 누적된 부채이고, **CONTEXT는 세션 간 상태의 단일 출처라 잘못 잘라내면 복구 비용이 크다.** 정리 자체가 판단(무엇이 아직 살아있는 결정인가)을 요구하므로 **전용 PR에서 집중해 처리한다.** 이번 PR에 끼워 넣으면 Stage 3a diff에 대규모 문서 이동이 섞여 리뷰가 흐려진다 |


## 처리 완료 (closed / wontfix / obsolete)

| ID | 출처 | 등급 | 내용 | 처리 |
|----|------|:----:|------|------|
| L-21 | `fix/ledger-triage` QA 부수 발견 (08-14) | MEDIUM | **`assert-qa-readonly.sh`의 세그먼트 분리기가 따옴표를 무시한다.** 명령 문자열을 정규식으로 `;` · `&&` · `&#124;&#124;` · `&#124;` 에서 쪼개는데 **따옴표 안인지 밖인지 보지 않는다.** 그래서 **패턴이나 구분자로 파이프 문자를 쓰는 정상 읽기 명령**까지 오탐 차단한다 — 예: `awk -F` 뒤에 파이프를 구분자로 주는 경우, `grep`에 이스케이프된 파이프로 OR 패턴을 주는 경우 | ✅ **closed — 2026-08-15.** 따옴표 안 문자를 마스킹한 사본에서 연산자를 찾고 값은 원문에서 꺼낸다. 🔴 **고치는 도중 회귀를 냈고 테스트가 잡았다** — 겹따옴표 안을 통째로 마스킹했더니 셸에서 살아있는 `$(...)`가 가려져 `mkdir -p "$(rm -rf be/core)"`가 뚫렸다. 홑따옴표만 완전 리터럴로 처리해 수정. 검증: 오탐 7건 해소 + **적대적 20건 그대로 차단**(스위트 99건) |
| L-11 | `chore/eks-persistent-guardrails/F-5` | LOW | **`.claude/scripts/`의 bash 로직에 자동 테스트가 없다.** 이번 PR이 `warn_orphan_volumes`(리퍼)와 영속 리소스 배너(세션 마커)를 추가했는데, 둘 다 회귀를 잡아줄 테스트가 없다. 리퍼는 **사람 없이 `tofu destroy -auto-approve`를 도는 스크립트**라 회귀의 대가가 다른 스크립트보다 크다 | ✅ **closed — 2026-08-15.** `test-guards.sh` + `tests/` 케이스 8파일(**99건**) 신설, `Design Integrity` CI에 연결(필수 체크). 🔑 **차단 케이스와 오탐 케이스를 함께** 돌린다 — 차단만 재면 *"전부 막으면 만점"*이 되어 가드가 조이는 방향으로만 회귀하고, 그러면 사람이 우회한다(L-21에서 실제 발생). 반증 2종 확인: 마스킹을 되돌리면 적대적 2건 FAIL, heredoc 면제를 빼면 오탐 프로브 1건 FAIL. **이 스위트는 만들어지는 도중에 이미 회귀를 하나 잡았다**(L-21 항목 참조) |
| L-20 | `fix/ledger-triage` (08-14 발견) | **HIGH** | 🔴 **위임 워크플로가 막혀 있다 — BE/FE 코드 작업 불가.** `assert-orchestrator-path.sh`는 경로만 보고 `be/`·`fe/`면 `exit 2`하는데 **에이전트 판별 로직이 없다.** `orchestrator.md` frontmatter에 등록됐지만 **서브에이전트가 상속**받아, `be-feature-builder`가 `assert-be-path`(be/ 허용)와 이 훅(be/ 차단)을 동시에 받아 차단된다. 실측: builder 스폰 → `be/...kt` Edit → `차단: orchestrator는 be/ 또는 fe/ 코드를 직접 수정할 수 없습니다` | ✅ **closed — 2026-08-14, `e09a88c`.** 훅 입력의 `agent_type`·`agent_id`로 판별(문서화된 필드). 🔴 **fail-closed로 짰다** — 순진하게 `agent_type != orchestrator → exit 0`으로 하면 필드가 없을 때(구버전·jq 부재·스키마 변경) **조용히 통과 = 가드 소멸**이다. 그래서 **서브에이전트라는 적극적 증거(두 필드 모두 존재)가 있을 때만** 면제한다. 판별 실패 시 위임이 막힐 뿐(시끄러운 실패) 가드가 사라지지 않는다. 검증: 합성 입력 **12/12**(fail-closed 2케이스 포함) + **실전 — builder 스폰이 차단 없이 통과**해 L-3·L-4를 실제로 고쳤다 |
| L-3 | `feat/phase1-ai-call-log-observability/F-3` | LOW | `AiCallLogObservabilityAdapter.record(log: AiCallLog)`의 **파라미터명 `log`가 클래스 멤버 로거를 섀도잉**. `this.log`로 해소돼 동작은 정상이나 가독성 저하 | ✅ **closed — 2026-08-14, `68d60ff`.** 구현체 파라미터명 `log`→`entry`, `this.` 우회 제거. **`db-core/AiCallLogAdapter.kt`의 동일 패턴도 함께** 고쳤다(한쪽만 고치면 다음 사람이 "왜 여긴 다르지"를 또 판단해야 한다). 인터페이스 `AiCallLogPort.record(log:)`는 보수적으로 미변경. 검증: `this.log` 잔존 **0건**, `:core:ai-api:test`·`:storage:db-core:test`·`:core:core-api:test` **exit=0**(orchestrator가 독립 재실행) |
| L-4 | `feat/phase1-ai-api-config-migration/F-1` | LOW | **core-api `application.yml` 주석의 근거 서술이 과장.** "지금 지우면 inprocess 롤백이 깨진다"고 적었으나, 실제로는 `@Value` 기본값이 명시값과 동일해 지금 지워도 동작은 같다. 진짜 근거는 "향후 default drift 대비" | ✅ **closed — 2026-08-14, `68d60ff`.** 주석을 사실대로 정정 — *"지금 지우면 롤백이 깨진다"*(거짓) → *"기본값(3)과 명시값(3)이 같아 지금 지워도 동작은 같다. 다만 한쪽만 바뀌면 조용히 갈라지므로(default drift) Phase 3까지 명시해 두는 편이 안전하다"*. **키 유지 결정 자체는 타당했고 문구만 틀렸다** |
| L-7 | `feat/phase1-core-http-adapter/F-4` | LOW | `be/clients/client-ai/build.gradle.kts`의 주석 *"core-api가 tools.jackson.databind.ObjectMapper 사용 중"*이 **1.4a 시점에 사실과 어긋났음** | ✅ **obsolete — 2026-08-14 실측으로 확정.** 주석이 *"core-api가 `tools.jackson.databind.ObjectMapper` 사용 중"*이라 주장하는데, `grep -rh "import .*ObjectMapper" be/core/core-api/src/main` → `tools.jackson.databind.ObjectMapper` **단일 결과**. 즉 1.4a 시점엔 거짓이었으나 **1.4b의 Jackson 3 되돌림으로 다시 참이 됐다.** 지적 자체가 소멸 |
| L-13 | `chore/design-integrity-content-assert/F-4` | LOW | **`.claude/qa-cache/`에 추적 중인 잔재 3개.** `.gitignore:48`에 등재돼 있으나 규칙 추가 전에 커밋된 파일이라 계속 추적된다(gitignore는 소급 적용 안 됨). `git ls-files .claude/qa-cache/` → 3건 | ✅ **closed — 2026-08-14.** `git rm --cached`로 3건 추적 해제. 반증: `git check-ignore -v` → `.gitignore:48:.claude/qa-cache/` 매칭 확인(추적 중이라 소급 적용이 안 됐던 것이지 규칙은 옳았다). `git ls-files .claude/qa-cache/` → **0건** |
| L-15 | `stage/eks-3b-verify/S-2` | ~~MEDIUM~~ | **readiness probe가 아무것도 검증하지 않는다.** `/health`가 상수 문자열만 반환하는데 readinessProbe가 그것을 본다 → DB가 죽어도 Ready → 트래픽 유입 → 전부 500 | ✅ **closed — 2026-08-10, PR #374.** readiness 그룹을 명시적으로 정의(`include: db,ping`)하고 probe를 `/actuator/health/readiness`로. **liveness는 `/health` 유지** — liveness가 DB를 보면 장애 시 전 파드 동시 재시작으로 **장애를 증폭**시킨다. 🔴 **착수 전 함정 하나를 잡았다**: `"/actuator/health"` 매처는 그 경로만 매칭해 `/actuator/health/readiness`가 `/actuator/**` IP 제한에 걸린다 → kubelet은 파드 IP로 찌르므로 **403 → 영영 Ready 안 됨**(고치려던 실패를 재현할 뻔). 테스트 4종 + **반증 3건 독립 실행**(설정/매처 제거 시 FAIL 확인) <br><br>🔬 **2026-08-12 유료 세션에서 실측 확인.** DB 정지 → T+30 파드 `0/1`·엔드포인트 제외(`notReadyAddresses`), **T+90까지 재시작 0**(liveness 20s×3 임계 초과) → *liveness가 의존성을 안 보는 설계*가 부재로 증명됨. 복구 후 수동 개입 없이 Ready 복귀. 🔎 실패 방식이 503이 아니라 **타임아웃**인 점은 L-19로 분리 |
| L-14 | `stage/eks-3b-verify/S-1` | ~~HIGH~~ | **`random_password.postgres`가 `2-cluster` state에 있어 클러스터를 destroy하면 in-cluster Postgres 로그인이 깨진다.** 볼륨은 `initdb` 때의 옛 해시를 유지하는데 state가 죽어 비밀번호가 새로 생성된다 → `FATAL: password authentication failed for user "devquest"` | ✅ **closed — 2026-08-10.** `random_password.postgres_master`를 **`0-bootstrap`으로 이동**(영속 EBS와 동일 수명, `prevent_destroy` + sensitive output). `tofu plan` 실측 **1 to add / 0 to change / 0 to destroy** — 순증이고 AWS 리소스가 아니라 **비용 0**. 기각한 대안: ①`ignore_changes`(state에서 사라지면 무시할 diff 자체가 없다 — $0 실증) ②Secrets Manager 별도 시크릿($0.40/월 영구 = 현재 영속비용의 44% 증가). **추가로 SOP §6b에 멱등 동기화 단계를 표준 절차로 넣었다** — 08-07 이전 볼륨은 어느 state에도 없는 옛 비밀번호를 들고 있고, 향후 회전 시에도 같은 갈라짐이 생기므로 *"한 번만 하는 수동 절차"*로 두지 않았다 <br><br>🔬 **2026-08-12 유료 세션에서 실측 확인.** 동기화 전 접속 실패(`FATAL: password authentication failed`) → `ALTER USER` 후 성공. 볼륨 데이터 생존(15 테이블·13 마이그레이션·최초적용 08-07). 🔴 **첫 검증은 무효였다** — `psql -h 127.0.0.1`은 `pg_hba.conf`상 `trust`라 어떤 비밀번호든 통과한다. 파드 IP로 붙어야 `scram-sha-256`을 탄다. SOP §6b·튜토리얼 3b-6에 명시함 |
| L-9 | `stage/eks-3a-postgres-dynamic-pvc/F-2` | ~~MEDIUM~~ | **`db_mode=rds` 경로가 실제 apply로 검증되지 않았다.** `one(aws_db_instance.main[*].master_user_secret[0].secret_arn)` 등 splat+중첩인덱스 표현식이 rds 모드에서 런타임에 올바른 ARN을 반환하는지 미확인 | ✅ **closed — 2026-08-07 유료 세션에서 실측 해소.** `tofu apply -var db_mode=rds` → `db_master_secret_arn = arn:aws:secretsmanager:ap-northeast-2:<account>:secret:rds!db-e0040ad0-…-n3Zsr3` (= `one()`이 런타임에 올바른 ARN 반환). 이어서 ESO가 그 **AWS 소유** 시크릿을 실제로 읽는 것까지 확인 — `SecretStore Ready=True store validated`, `externalsecret core-api-db SecretSynced`, Flyway 13개 적용 + `/health` 200. **D-001이 `rds.tf`를 남겨둔 근거("Stage 2 재현성")의 전제가 성립함이 확정됐다.** 상세: `docs/eks-migration-log.md` 08-07 |
| L-10 | `fix/timezone-consistency/F-2` | LOW | `-Duser.timezone` 수정을 실제 Alpine 이미지로 검증하지 못했음(개발 머신에 docker 없음) | ✅ **closed — #337에서 실측 완료.** colima 설치 후 `eclipse-temurin:21-jre/jdk-alpine`을 직접 실행해 확정. 배포 후 확인(TASKS TASK-8) **불필요**해져 해당 태스크도 종결 |
| L-9 | `fix/daily-question-window/F-2` | ~~LOW~~ | 🔴 **obsolete — 오진. 이 결함은 존재한 적이 없다.**<br>주장했던 내용: *"저장은 ambient zone, 조회는 명시적 KST라 어긋나서 ①데일리 메일이 09:00 cron에 우연히만 맞고 ②코딩 스트릭이 00:00~09:00 제출을 전날로 기록한다."* → **둘 다 사실이 아니다.** prod는 `TZ=Asia/Seoul`(#210, fly.toml / #324, k8s)로 **처음부터 KST**였으므로 저장·조회가 어긋난 적이 없다. | **#337에서 실측으로 반증.** `eclipse-temurin:21-*-alpine` 직접 실행: `TZ` 미설정 → `GMT` / **`TZ=Asia/Seoul` → `Asia/Seoul`** (이 이미지엔 tzdata가 **포함**돼 있다).<br>🔴 **오진 경로 2단계**: ①`be/fly.toml`이 아니라 루트 `fly.toml`을 grep → 파일 부재로 `\|\|` 폴백의 "없음"이 출력됐고 이를 검증된 사실로 보고 ②QA가 `TZ` 존재를 찾아낸 뒤, **우리 이미지를 재보는 대신 웹 검색의 일반론**("Alpine엔 tzdata 없음")을 채택 — 기존 결론을 살려주는 방향이라 더 위험했다.<br>**교훈: 일반론으로 특정 환경의 사실을 대체하지 말 것. `docker run` 한 줄이면 30초다.** (#336의 코드 변경 자체는 유지 — 아래 L-9-c) |
| L-9-b | `fix/timezone-consistency/F-3` | ~~LOW~~ | 배포 전/후 저장값 **불연속**(백필 미실시) | 🔴 **obsolete — 전제가 무너졌다.** prod가 원래 KST였으므로 #336 배포 전후로 저장 zone이 바뀌지 않는다 = **불연속 자체가 없다** |
| L-9-c | (#336 잔존 가치) | — | 오진이었지만 **#336의 코드 변경은 유지할 가치가 있다** | ✅ **유지 확정(#337).** ①`be/build.gradle.kts` 테스트 jvmArgs — CI 러너는 UTC이고 TZ가 없어 **테스트는 UTC, prod는 KST**로 돌고 있었다. 이건 실재하던 CI≢prod 갭이고 이 변경이 닫았다 ②`TimezoneConsistencyTest` — 위 갭의 회귀 가드 ③Dockerfile `-Duser.timezone` — TZ와 중복이지만 **배포 설정이 유실돼도 버티는 이중 안전장치**로 유지(주석을 사실대로 정정) |

---

## 이미 다른 곳에 기록된 것 (원장 중복 등재 안 함)

아래는 `.claude/CONTEXT.md`의 **백로그 / 다음 작업** 섹션이 소유한다. 원장에 이중 등재하지 않는다.

- `CodingQuestService.generateProblem`/`submitCode` 트랜잭션 재배치 보류 (#308 MEDIUM) → CONTEXT "서비스 분해 에픽" 잔존 리스크
- `be/core/core-api/src/main/resources/application-prod.yml`의 Boot 3 잔재 키 `server.error.*` (#308 LOW) → CONTEXT 백로그
- 죽은 설정 `devquest.ai.pass-score`·`interview-questions` (#306 부수 발견) → CONTEXT 백로그
- Jackson 2 잔재 (`CodingQuestService`·인터셉터 2종) (#308 LOW) → CONTEXT 백로그
